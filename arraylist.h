//arraylist.h
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 사용자정의 헤더파일
// 구조체, 함수들의 원형을 작성
void printlist(char**);
void append(char**, const char*);
void prepend(char**, const char*);